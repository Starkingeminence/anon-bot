# =============================================================================
# Acki Nacki DAO Bot — Environment Variables
# Copy this file to .env for local dev. Set these as Render Environment Variables
# in the Render dashboard for production (never commit .env to git).
# =============================================================================

# ── Telegram ──────────────────────────────────────────────────────────────────
BOT_TOKEN=7123456789:AAHxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Your Render service public URL (no trailing slash). Used to build WEBHOOK_URL.
WEBHOOK_HOST=https://your-service-name.onrender.com

# ── Supabase PostgreSQL ───────────────────────────────────────────────────────
# Find in Supabase dashboard: Project Settings → Database → Connection String (URI mode)
# Use the "Direct connection" string (port 5432), NOT the pooler, so asyncpg manages pooling.
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ── Upstash Redis ─────────────────────────────────────────────────────────────
# Find in Upstash console: your database → Details → Redis URL (rediss:// for TLS)
REDIS_URL=rediss://default:[YOUR-PASSWORD]@[YOUR-ENDPOINT].upstash.io:6379

# ── Bot Configuration ─────────────────────────────────────────────────────────
# Comma-separated Telegram user_ids of the 4 admins (no spaces)
ADMIN_IDS=111111111,222222222,333333333,444444444
# Telegram chat_id of the main DAO group (negative number for groups, e.g. -1001234567890)
GROUP_ID=-1001234567890
# Acki Nacki wallet address for the Founder's 1% payout
FOUNDER_WALLET=0:abc123...
# Hot wallet address (bot's secondary wallet — holds limited Smart Contract Allowance)
HOT_WALLET_ADDRESS=0:def456...
# Acki Nacki Multi-Sig treasury wallet (read-only for /treasury command)
MULTISIG_WALLET_ADDRESS=0:ghi789...
# Acki Nacki RPC endpoint (mainnet GraphQL)
ACKI_NACKI_RPC=https://mainnet.ackinacki.org/graphql

# ── Render Port (set automatically by Render — do not change) ─────────────────
PORT=8080
